server:
  port: 5001

spring:
  application:
    name: USER-SERVICE

  jpa:
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:mysql://localhost:3306/user_service
    username: root
    password: ''
    driver-class-name: com.mysql.cj.jdbc.Driver
